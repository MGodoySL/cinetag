import styles from "./Rodape.module.css";

export const Rodape = () => {
    return (
        <footer className={styles.rodape}>
            <h2>Desenvolvido por Matheus Godoy</h2>
        </footer>
    );
};
